apiVersion: v1
kind: ReplicationController
metadata:
  name: nginx
  labels:
    app: nginx
    tier: dev
spec:
  template:
    metadata:
      name: nginx
    spec:
      containers:
        - name: nginx
          image: nginx:alpine
  replicas: 3
